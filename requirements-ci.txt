# CI dependencies.
#
# PsychoPy is deliberately absent: a headless runner has no audio device and
# no display, so the tests that need it are marked `psychopy` and skipped
# there (they still run on the development machine).  Keeping PsychoPy out
# also keeps CI honest about the mcgurk/ package staying importable without it.
#
# Versions must match requirements.txt / requirements-dev.txt.

pydantic==2.13.4
pyyaml==6.0.3

# mcgurk/stimuli — the DSP that decides burst times, levels, SNR and GIN gap
# placement is tested against synthetic signals, which needs no ffmpeg and no
# audio device.  The tests that do shell out to ffmpeg are marked `ffmpeg`.
numpy==2.2.6
scipy==1.14.1
soundfile==0.13.1

# mcgurk/analysis — export builds pandas DataFrames from the flat view and the
# raw tables; the measures layer wires each module's numbers to the database.
# Both are PsychoPy-free and CI-tested.  Parquet (pyarrow) stays optional — CSV
# is what CI exercises.
pandas==2.3.3

pytest==9.1.1
ruff==0.16.0
mypy==2.3.0
