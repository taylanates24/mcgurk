# Exact pins.  A psychophysics study runs for months across several machines;
# a silent minor-version bump in PsychoPy or its audio/video stack can shift
# timing without producing any visible error.  The versions below are the ones
# verified on the development machine (Python 3.10, Windows 11) — see README.md.

# --- Experiment engine ---
psychopy==2026.1.2
psychtoolbox==3.0.19.14     # ptb audio backend — required, no fallback allowed
sounddevice==0.5.5          # audio device enumeration in the admin dialog
ffpyplayer==4.5.3           # MovieStim video decoding
pyglet==1.4.11              # PsychoPy window backend
pillow==11.3.0              # image handling
imageio-ffmpeg==0.6.0       # bundled ffmpeg when none is on PATH

# --- Config & data ---
pyyaml==6.0.3
pydantic==2.13.4            # config schema validation (mcgurk/config)
numpy==2.2.6
scipy==1.14.1
pandas==2.3.3
soundfile==0.13.1           # 24-bit PCM WAV I/O (scipy can only do 16/32-bit)

# --- Operator panel ---
# PyQt6 (not PySide6): PsychoPy's own dialogs (psychopy.gui) import PyQt
# directly, and PyInstaller cannot bundle two Qt bindings, so the panel uses the
# same binding PsychoPy needs.  PsychoPy already pulls PyQt6 in; pinned here.
PyQt6==6.10.2
